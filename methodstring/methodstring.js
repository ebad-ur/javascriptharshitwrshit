
// this is trim method which is use to remove the spaces from the program  
// declare variable
let first_names = "           ebad          "

// find the length of program 
console.log(first_names.length);

//use trim method for removing spaces 
first_names = first_names.trim();

// print after applying method 
console.log(first_names);

// checvking length again 
console.log(first_names.length);

// this is the upper case which is use to convert all the letter in captial 

// intiliaze  the variable 
let firstName = "ebad ur rehman"

// printing the value 
console.log(firstName)

// using the touppercasemethod() 
firstName = firstName.toUpperCase();

//printing the value again 
console.log(firstName)

// intilize the value 
let firstebad = "EBAD";

// print the vlaue 
console.log(firstebad);

// applying the tolwercase method()
firstebad = firstebad.toLowerCase();

// printing value again
console.log(firstebad);

//using slice method()
let firstnames$ = "ebad ur rehman"

//printing the value 
console.log(firstnames$);


//using slice() method for this we make new variable 

let newString = firstnames$.slice(6,8);
console.log(newString);